version: 2
updates:
  # Keeps the SHA-pinned actions in the workflows current; the plugin has no package dependencies.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    groups:
      actions:
        patterns: ['*']
